{
    "name": "PreFigure-Codespaces",
    "dockerFile": "Dockerfile",
    "postCreateCommand": "prefig init && prefig examples && prefig new && && cat .bashrc >> ~/.bashrc && . ~/.bashrc",

    "customizations": {
        "codespaces": {
            "openFiles": ["samples/tangent.xml"]
        },
        "vscode": {
            "extensions": [
                "vitaliymaz.vscode-svg-previewer"
            ]
        }
    }
}
