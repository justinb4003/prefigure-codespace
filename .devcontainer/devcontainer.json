{
    "name": "PreFigure-Codespaces",
    "dockerFile": "Dockerfile",
    "postCreateCommand": "prefig_init && prefig_examples",

    "customizations": {
        "codespaces": {
            "openFiles": ["samples/tangent.xml"]
        },
        "vscode": {
            "extensions": [
                "vitaliymaz.vscode-svg-previewer"
            ]
        }
    }
}
